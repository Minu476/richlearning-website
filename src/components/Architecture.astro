---
/**
 * Architecture Section — "No Hidden Layers. Just Geometry."
 * 3-column feature grid: O(1) Inference, Zero Forgetting, Auditable Paths
 */
---

<section class="arch-section" id="architecture">
  <div class="section">
    <p class="section-label">// The Architecture</p>
    <h2 class="section-title">No Hidden Layers. Just Geometry.</h2>
    <p class="section-subtitle">
      We replaced the Neural Network with a Topological Graph.
      Knowledge lives as nodes and edges — not frozen weights.
    </p>

    <div class="feature-grid">
      <div class="feature-card">
        <div class="feature-icon">⚡</div>
        <h3 class="feature-title">O(1) Inference</h3>
        <p class="feature-subtitle">Speed of Light</p>
        <p class="feature-body">
          Inference time is constant. It doesn't matter how much data you have —
          the agent only looks at the immediate neighborhood. Pointer traversal, not matrix math.
        </p>
      </div>

      <div class="feature-card">
        <div class="feature-icon">🧠</div>
        <h3 class="feature-title">Zero Forgetting</h3>
        <p class="feature-subtitle">Infinite Memory</p>
        <p class="feature-body">
          New experiences add nodes to the graph. They never overwrite existing knowledge.
          Learn Task B without breaking Task A. Continual learning by design.
        </p>
      </div>

      <div class="feature-card">
        <div class="feature-icon">🔍</div>
        <h3 class="feature-title">Auditable Paths</h3>
        <p class="feature-subtitle">Total Transparency</p>
        <p class="feature-body">
          Debug your AI by tracing the path. See exactly which "Landmarks" led to
          the decision. Every step recorded. No more guessing.
        </p>
      </div>
    </div>

    <!-- Mini graph diagram -->
    <div class="arch-diagram">
      <div class="diagram-wrapper">
        <div class="diagram-col">
          <div class="diagram-label">Deep Neural Network</div>
          <div class="dense-net">
            <div class="dn-row">
              <span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span>
            </div>
            <div class="dn-mesh"></div>
            <div class="dn-row">
              <span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span>
            </div>
            <div class="dn-mesh"></div>
            <div class="dn-row">
              <span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span>
            </div>
            <div class="dn-mesh"></div>
            <div class="dn-row">
              <span class="dn-node"></span><span class="dn-node"></span><span class="dn-node"></span>
            </div>
          </div>
          <p class="diagram-caption">25M parameters. All fire on every query.</p>
        </div>

        <div class="diagram-vs">→</div>

        <div class="diagram-col">
          <div class="diagram-label diagram-label-rl">Topological Graph Memory</div>
          <div class="topo-graph">
            <svg viewBox="0 0 240 140" xmlns="http://www.w3.org/2000/svg">
              <!-- Edges (inactive) -->
              <line x1="30" y1="70" x2="80" y2="30" stroke="#333" stroke-width="1"/>
              <line x1="30" y1="70" x2="80" y2="110" stroke="#333" stroke-width="1"/>
              <line x1="80" y1="30" x2="140" y2="50" stroke="#333" stroke-width="1"/>
              <line x1="80" y1="110" x2="140" y2="90" stroke="#333" stroke-width="1"/>
              <line x1="140" y1="50" x2="200" y2="70" stroke="#333" stroke-width="1"/>
              <line x1="140" y1="90" x2="200" y2="70" stroke="#333" stroke-width="1"/>
              <line x1="80" y1="30" x2="80" y2="110" stroke="#333" stroke-width="1"/>
              <line x1="140" y1="50" x2="140" y2="90" stroke="#333" stroke-width="1"/>
              <!-- Active path -->
              <line x1="30" y1="70" x2="80" y2="30" stroke="#00ff88" stroke-width="2.5" opacity="0.9"/>
              <line x1="80" y1="30" x2="140" y2="50" stroke="#00ff88" stroke-width="2.5" opacity="0.9"/>
              <line x1="140" y1="50" x2="200" y2="70" stroke="#00ff88" stroke-width="2.5" opacity="0.9"/>
              <!-- Nodes (inactive) -->
              <circle cx="80" cy="110" r="6" fill="#444"/>
              <circle cx="140" cy="90" r="6" fill="#444"/>
              <!-- Nodes (active path) -->
              <circle cx="30" cy="70" r="7" fill="#00cc6a"/>
              <circle cx="30" cy="70" r="11" fill="none" stroke="rgba(0,255,136,0.3)" stroke-width="2"/>
              <circle cx="80" cy="30" r="7" fill="#00cc6a"/>
              <circle cx="140" cy="50" r="7" fill="#00cc6a"/>
              <circle cx="200" cy="70" r="8" fill="#00ff88"/>
              <circle cx="200" cy="70" r="12" fill="none" stroke="rgba(0,255,136,0.4)" stroke-width="2"/>
              <!-- Labels -->
              <text x="30" y="90" fill="#888" font-size="9" text-anchor="middle" font-family="monospace">Start</text>
              <text x="200" y="90" fill="#00ff88" font-size="9" text-anchor="middle" font-family="monospace">Target</text>
            </svg>
          </div>
          <p class="diagram-caption diagram-caption-rl">6 nodes. Only the path fires.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .arch-section { background: var(--void); }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin: 3rem 0;
  }
  .feature-card {
    padding: 1.75rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    transition: border-color 0.2s;
  }
  .feature-card:hover { border-color: var(--accent-dim); }
  .feature-icon { font-size: 2rem; margin-bottom: 0.75rem; }
  .feature-title {
    font-size: 1.15rem; font-weight: 600;
    color: var(--text); margin-bottom: 0.15rem;
  }
  .feature-subtitle {
    font-family: var(--mono);
    font-size: 0.75rem;
    color: var(--accent);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 0.75rem;
  }
  .feature-body {
    font-size: 0.9rem;
    color: var(--text-dim);
    line-height: 1.6;
  }

  /* ═══ Architecture Diagram ═══ */
  .arch-diagram {
    margin-top: 3rem;
    padding: 2rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
  }
  .diagram-wrapper {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 2rem;
    align-items: center;
  }
  .diagram-col { text-align: center; }
  .diagram-label {
    font-family: var(--mono);
    font-size: 0.7rem;
    color: var(--text-dim);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
  }
  .diagram-label-rl { color: var(--accent); }
  .diagram-vs {
    font-size: 1.5rem;
    color: var(--text-dim);
    font-family: var(--mono);
  }
  .diagram-caption {
    font-size: 0.8rem;
    color: var(--text-dim);
    margin-top: 0.75rem;
    font-style: italic;
  }
  .diagram-caption-rl { color: var(--accent-dim); }

  /* Dense neural net visual */
  .dense-net {
    display: flex; flex-direction: column;
    align-items: center; gap: 2px;
    padding: 1.5rem;
    background: var(--void);
    border-radius: 8px;
    border: 1px solid var(--border);
  }
  .dn-row {
    display: flex; gap: 6px;
    justify-content: center;
  }
  .dn-node {
    width: 10px; height: 10px;
    background: rgba(255,68,68,0.5);
    border-radius: 50%;
    animation: dnPulse 2s ease-in-out infinite;
  }
  .dn-node:nth-child(even) { animation-delay: 0.3s; }
  .dn-mesh {
    width: 100%; height: 16px;
    background: repeating-linear-gradient(
      45deg,
      transparent,
      transparent 3px,
      rgba(255,68,68,0.08) 3px,
      rgba(255,68,68,0.08) 4px
    ),
    repeating-linear-gradient(
      -45deg,
      transparent,
      transparent 3px,
      rgba(255,68,68,0.08) 3px,
      rgba(255,68,68,0.08) 4px
    );
  }
  @keyframes dnPulse {
    0%, 100% { opacity: 0.4; }
    50% { opacity: 1; }
  }

  /* Topo graph */
  .topo-graph {
    padding: 0.5rem;
    background: var(--void);
    border-radius: 8px;
    border: 1px solid rgba(0,255,136,0.15);
  }
  .topo-graph svg { width: 100%; height: auto; }

  @media (max-width: 768px) {
    .feature-grid { grid-template-columns: 1fr; }
    .diagram-wrapper { grid-template-columns: 1fr; }
    .diagram-vs { transform: rotate(90deg); }
  }
</style>

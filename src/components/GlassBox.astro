---
/**
 * Glass Box Section — Explainability comparison
 * Deep Learning: "Black Box" vs Rich Learning: "Glass Box" with visible path
 */
---

<section class="glassbox-section" id="how-it-works">
  <div class="section">
    <p class="section-label">// Explainability</p>
    <h2 class="section-title">The Glass Box Challenge</h2>
    <p class="section-subtitle">
      Deep Learning hides its reasoning behind billions of parameters.
      Rich Learning shows you the exact path — every step, every decision.
    </p>

    <div class="box-grid">
      <!-- Black Box -->
      <div class="box-card box-black">
        <div class="box-header">
          <span class="box-badge box-badge-old">Deep Learning</span>
        </div>
        <div class="box-body">
          <div class="black-box-visual">
            <div class="bb-input">Query: "Who is this?"</div>
            <div class="bb-middle">
              <div class="bb-layer"></div>
              <div class="bb-layer"></div>
              <div class="bb-layer"></div>
              <div class="bb-dots">• • •</div>
              <div class="bb-layer"></div>
              <div class="bb-layer"></div>
            </div>
            <div class="bb-output">
              <span class="bb-result">Match: Person A</span>
              <span class="bb-confidence">Confidence: 99.97%</span>
              <span class="bb-why">Why? <span class="bb-unknown">Unknown</span></span>
            </div>
          </div>
        </div>
        <div class="box-footer">
          <span class="box-stat">50+ hidden layers</span>
          <span class="box-stat">25M parameters</span>
          <span class="box-stat">No audit trail</span>
        </div>
      </div>

      <!-- Glass Box -->
      <div class="box-card box-glass">
        <div class="box-header">
          <span class="box-badge box-badge-new">Rich Learning</span>
        </div>
        <div class="box-body">
          <div class="glass-box-visual">
            <div class="gb-input">Query: "Who is this?"</div>
            <div class="gb-path">
              <div class="gb-step">
                <span class="gb-node">Start</span>
                <span class="gb-arrow">→</span>
              </div>
              <div class="gb-step gb-trap">
                <span class="gb-node">Trap</span>
                <span class="gb-label">⚠ Loop detected</span>
                <span class="gb-arrow">↩</span>
              </div>
              <div class="gb-step">
                <span class="gb-node">Node B</span>
                <span class="gb-arrow">→</span>
              </div>
              <div class="gb-step">
                <span class="gb-node">Node C</span>
                <span class="gb-arrow">→</span>
              </div>
              <div class="gb-step gb-target">
                <span class="gb-node">Target ✓</span>
              </div>
            </div>
            <div class="gb-output">
              <span class="bb-result">Match: Person A</span>
              <span class="bb-confidence" style="color: var(--accent);">Confidence: 100%</span>
              <span class="bb-why">Why? <span class="gb-known">Path: Start → B → C → Target</span></span>
            </div>
          </div>
        </div>
        <div class="box-footer">
          <span class="box-stat">0 hidden layers</span>
          <span class="box-stat">Pure arithmetic</span>
          <span class="box-stat">Full audit trail</span>
        </div>
      </div>
    </div>

    <div class="glassbox-quote">
      <blockquote>
        "Every decision is a path you can trace backwards. Every mistake is a lesson you can point to."
      </blockquote>
    </div>
  </div>
</section>

<style>
  .glassbox-section {
    background: var(--void);
  }
  .box-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin: 3rem 0;
  }
  .box-card {
    border-radius: 12px;
    border: 1px solid var(--border);
    overflow: hidden;
    background: var(--surface);
  }
  .box-header {
    padding: 1rem 1.5rem;
    border-bottom: 1px solid var(--border);
  }
  .box-badge {
    font-family: var(--mono);
    font-size: 0.75rem;
    padding: 0.25rem 0.75rem;
    border-radius: 4px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .box-badge-old { background: rgba(255,68,68,0.15); color: var(--danger); }
  .box-badge-new { background: var(--accent-glow); color: var(--accent); }
  .box-body { padding: 1.5rem; }
  .box-footer {
    padding: 1rem 1.5rem;
    border-top: 1px solid var(--border);
    display: flex; gap: 1rem; flex-wrap: wrap;
  }
  .box-stat {
    font-family: var(--mono);
    font-size: 0.7rem;
    color: var(--text-dim);
    padding: 0.2rem 0.5rem;
    background: var(--void);
    border-radius: 4px;
  }

  /* ═══ Black Box Visual ═══ */
  .black-box-visual { display: flex; flex-direction: column; gap: 1rem; }
  .bb-input, .gb-input {
    font-family: var(--mono);
    font-size: 0.8rem;
    color: var(--text-dim);
    padding: 0.5rem 0.75rem;
    background: var(--void);
    border-radius: 6px;
    border: 1px solid var(--border);
  }
  .bb-middle {
    display: flex; flex-direction: column; gap: 4px;
    padding: 1rem;
    background: repeating-linear-gradient(
      0deg, var(--void), var(--void) 2px, var(--surface-2) 2px, var(--surface-2) 4px
    );
    border-radius: 6px;
    opacity: 0.6;
  }
  .bb-layer {
    height: 8px;
    background: linear-gradient(90deg, rgba(255,68,68,0.3), rgba(255,68,68,0.1));
    border-radius: 2px;
  }
  .bb-dots {
    text-align: center; color: var(--text-dim);
    font-size: 0.8rem; padding: 0.25rem 0;
  }
  .bb-output {
    display: flex; flex-direction: column; gap: 0.25rem;
    font-family: var(--mono); font-size: 0.8rem;
  }
  .bb-result { color: var(--text); }
  .bb-confidence { color: var(--danger); }
  .bb-why { color: var(--text-dim); }
  .bb-unknown { color: var(--danger); font-style: italic; }

  /* ═══ Glass Box Visual ═══ */
  .glass-box-visual { display: flex; flex-direction: column; gap: 1rem; }
  .gb-path {
    display: flex; align-items: center; gap: 0.25rem;
    flex-wrap: wrap; padding: 1rem;
    background: rgba(0,255,136,0.03);
    border: 1px solid rgba(0,255,136,0.1);
    border-radius: 6px;
  }
  .gb-step { display: flex; align-items: center; gap: 0.25rem; }
  .gb-node {
    font-family: var(--mono); font-size: 0.75rem;
    padding: 0.3rem 0.6rem; border-radius: 4px;
    background: var(--surface-2); border: 1px solid var(--border);
    color: var(--text);
  }
  .gb-trap .gb-node {
    border-color: rgba(255,68,68,0.5);
    color: var(--danger);
    text-decoration: line-through;
  }
  .gb-label {
    font-family: var(--mono); font-size: 0.6rem;
    color: var(--danger);
  }
  .gb-target .gb-node {
    border-color: var(--accent);
    color: var(--accent);
    font-weight: 600;
  }
  .gb-arrow {
    color: var(--accent); font-size: 0.8rem;
    font-family: var(--mono);
  }
  .gb-trap .gb-arrow { color: var(--danger); }
  .gb-known { color: var(--accent); font-weight: 500; }

  /* ═══ Quote ═══ */
  .glassbox-quote {
    margin-top: 2rem;
    padding: 1.5rem 2rem;
    border-left: 3px solid var(--accent);
    background: var(--accent-glow);
    border-radius: 0 8px 8px 0;
  }
  .glassbox-quote blockquote {
    font-size: 1.05rem;
    font-style: italic;
    color: var(--text);
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .box-grid { grid-template-columns: 1fr; }
    .gb-path { flex-direction: column; align-items: flex-start; }
  }
</style>
